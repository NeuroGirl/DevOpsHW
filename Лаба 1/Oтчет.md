# Лабораторная работа №1: Docker (Advanced)

## Задание:

Вариант 2: Разработайте утилиту на любом удобном языке программирования (например, Go, Python), которая запускает команду в контейнере.

Должна конфигурироваться config.json по спецификации OCI

Для каждого контейнера при его запуске должны создаваться новые namespaces:

PID namespace;

Mount namespace;

UTS namespace, внутри которого hostname устанавливается в значение из поля hostname конфига.

Для каждого контейнера с идентификатором <id> должна создаваться директория: /var/lib/{имя-вашей-утилиты}/{id}

В качестве rootfs использовать Alpine, но chroot делать на overlayfs:

lowerdir — базовый rootfs (Alpine)

upperdir — /var/lib/{имя-утилиты}/{id}/upper

workdir — /var/lib/{имя-утилиты}/{id}/work

merged - /var/lib/{имя-утилиты}/{id}/merged

Запускаемая команда становится PID=1 внутри контейнера и утилита ждёт её завершения (foreground)

Опционально:

1) Настроить cgroups для ограничения ресурсов контейнера (CPU, память, IO).
2) Внутри контейнера монтировать /proc для корректной работы утилит типа ps

### Ход работы: 

1) Установить alpine mini root filesystem и распаковать через tar в заранее созданную папку tmp/alpine/rootfs. 

<img width="738" height="452" alt="Снимок экрана 2026-03-31 203629" src="https://github.com/user-attachments/assets/19c4179a-1b2e-4c9d-bd9b-a57070ed7bf0" />

<img width="741" height="124" alt="Снимок экрана 2026-03-31 204033" src="https://github.com/user-attachments/assets/2572ab97-37fd-474e-9464-2f3e3c1f16da" />

Для проверки правильности установленного просмотрим содержание папки rootfs - если наш корень файловой системы действительно имеет папки, типичные для Alpine (и файловых систем впринципе, так как они
строятся по плюс-минус одному принципу), то все хорошо.

<img width="518" height="147" alt="image" src="https://github.com/user-attachments/assets/da497c72-8252-4d45-a97b-1d94707cb626" />

Отдельная файловая система нужна для задания базового слоя, на который будут накладываться происходящие отдельно в каждом контейнере изменения. Таким образом контейнеры не будут портить базовый образ, а основная файловая сисетма устройства будет изолирована от контейнера и контейнер не сможет внести в нее хаос

2) Написать config.json и script.py

Код можно просмотреть в соответсвующих приложенных файлах. Пояснения к кодам:

config.json

script.py

Не забываем, что эти файлы мы создаем в основной ОС, а не внутри контейнера! 

3) Проверяем работу:

Чтобы проверить код, надо его запустить (неочевидно, да?). Запускаем с помощью команды sudo python3 script.py run [имя которое мы хотим дать контейнеру].

<img width="674" height="426" alt="image" src="https://github.com/user-attachments/assets/b170d098-a806-470a-a311-3859eb472d53" />

Таким образом мы оказываемся внутри контейнера и можем с ним работать, обращаясь к нему, как к мини ОС - а значит и сравнить результаты различных команд, примененных из ОС и из контейнера, чтобы проверить изолированность

<img width="674" height="426" alt="image" src="https://github.com/user-attachments/assets/d6218767-0ba0-4b9d-ad8b-8b6b2b88e435" />

Скриншоп показывает нам, что внутри контейнера мы видим свое отдельное имя хоста, отдельную изолированную файловую систему и свой отдельный набор процессов. 
Аналогичные команды выполняем из основной ОС и проверим, где в основной ОС находятся созданные изнутри контейнера файлы - он должен находиться в папке с новыми слоями, куда мы перенаправляем сохранения из контейнеров (var/lib/my_tool/[имя которое мы дали контейнеру]/upper)

<img width="729" height="463" alt="image" src="https://github.com/user-attachments/assets/f4cb6251-01d2-4642-a434-43d9d70949ab" />

<img width="631" height="72" alt="image" src="https://github.com/user-attachments/assets/30be010c-857b-4dc0-80e9-3d628d64fb90" />

### Выполнила: Сусликова В.Д. 467632
